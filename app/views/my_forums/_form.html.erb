<%= form_for(@my_forum) do |f| %>
  <% if @my_forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_forum.errors.count, "error") %> prohibited this my_forum from being saved:</h2>

      <ul>
      <% @my_forum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
